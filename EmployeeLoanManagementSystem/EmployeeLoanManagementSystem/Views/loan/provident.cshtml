@model System.Data.DataTable

@{
    ViewBag.Title = "provident";
}
@using (Html.BeginForm())
{
    <h2>LOAN AGAINST PROVIDENT FUND</h2>
    <table class="table table-bordered table-striped">
        <tr>
            <th >
                Loan Apply Id
            </th>
            <th>Loan Money</th>
            <th>Request Date</th>
            <th>Loan for Property</th>
            <th>Loan for Automobile</th>
            <th>Edit Loan</th>
            <th>Verify Document</th>
        </tr>
        @for (int i = 0; i < Model.Rows.Count; i++)
        {
            <tr>
                <td name="loanid">@Model.Rows[i][0]</td>
                <td>@Model.Rows[i][1]</td>
                <td>@Model.Rows[i][2]</td>
                <td>@Model.Rows[i][3]</td>
                <td>@Model.Rows[i][4]</td>
                <td>@Html.ActionLink("Edit", "Edit",  new{ ide = @Model.Rows[i][0] },null)</td>
                <td>@Html.ActionLink("Documents", "documentsverify", "loan", new { ide = @Model.Rows[i][0] },null)</td>
            </tr>
        }
    </table>
}
